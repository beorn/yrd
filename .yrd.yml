# Yrd's own queue. Custom checks are defined inline in the required checks list.
checks:
  - verify:
      run: bun run verify:component

contest:
  concurrency: 2
  timeoutMs: 1800000
  evaluators: [verify]
